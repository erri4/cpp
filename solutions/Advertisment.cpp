#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    ll n;
    cin >> n;
    vector<ll> arr(n);
    for (ll i = 0; i < n; i++) cin >> arr[i];
    ll mx = 0;
    ll rect1; ll rect2;
    for (ll i = 0; i < n; i++){
        
    }
    return 0;
}