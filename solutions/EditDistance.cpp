#include <iostream>
#include <vector>

#define print(data) (std::cout << data << '\n')
#define input(var) (std::cin >> var)

using ll = long long;
using str = std::string;
using std::vector;
using std::max;

ll EditDistance(str n, str m){
    
}

int main(){
    str n;
    str m;
    input(n);
    input(m);
    print(EditDistance(n, m));
    return 0;
}