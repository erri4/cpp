#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll n;
    cin >> n;
    vector<ll> cards(n);
    for (ll i = 0; i < n; i++) cin >> cards[i];
    for (ll i = 0; i < n; i++){
        if (i < n/2){
            
        }
        else {}
    }
    return 0;
}